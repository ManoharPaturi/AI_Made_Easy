"""Keras code template (functional API — required for merge blocks).

The IR is channels-first; Keras is channels-last. Translation happens in
the generator (input shapes reversed) and in block fragments (axis params,
Permute/Reshape targets are pre-translated by their shape/emission rules).
"""

KERAS_TEMPLATE = '''\
"""{{ graph_name }} - generated by AI Made Easy."""
import keras
from keras import layers


def build_model() -> keras.Model:
    inputs = keras.Input(shape={{ keras_input_shape }})
{% for n in nodes %}
    {{ n.keras_expr }}
{% endfor %}
    return keras.Model(inputs=inputs, outputs={{ keras_output_var }})


if __name__ == "__main__":
    model = build_model()
    model.summary()
'''
